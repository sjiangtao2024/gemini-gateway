# AI-Gateway 项目总结

> **版本**: 1.0.0  
> **完成日期**: 2026-02-08  
> **状态**: ✅ 开发完成

---

## 📋 项目概述

AI-Gateway 是一个支持 **OpenAI** 和 **Claude** 双协议的 AI 模型统一网关，提供对 **Gemini** 和 **GPT4Free (g4f)** 多 Provider 的透明接入。

### 核心特性

| 特性 | 说明 |
|-----|------|
| 🤖 多模型支持 | Gemini 2.5/3.0 + g4f 聚合模型 (Qwen, Kimi, GLM, Minimax, Grok 等) |
| 🔄 双协议兼容 | OpenAI (`/v1/chat/completions`) + Claude (`/v1/messages`) |
| 📡 多模态 | Vision 图像理解、文件上传分析、图像生成 |
| 🔧 配置热重载 | 修改配置无需重启，自动生效 |
| 📊 请求日志 | 自动记录所有请求方法和耗时 |
| 🔐 Bearer 认证 | 标准 Token 认证 |
| 🍪 Cookie 管理 | API 接口更新 Cookie，支持自动刷新 |

---

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│   OpenAI 客户端 (Codex CLI)    Claude 客户端 (Claude Code)   │
└──────────────────┬──────────────────────┬───────────────────┘
                   │                      │
                   ▼                      ▼
┌─────────────────────────────────────────────────────────────┐
│                    AI-Gateway (FastAPI)                      │
│  ┌─────────────────────┐    ┌─────────────────────────────┐ │
│  │   /v1/ (OpenAI协议)  │    │   /v1/messages (Claude协议)  │ │
│  │   /v1/models        │    │   /v1/claude/models          │ │
│  │   /v1/images        │    │                              │ │
│  │   /v1/files         │    │                              │ │
│  └──────────┬──────────┘    └──────────────┬──────────────┘ │
│             │                               │               │
│             └──────────────┬───────────────┘                │
│                            ▼                               │
│                   ┌─────────────────┐                      │
│                   │   模型路由引擎    │                      │
│                   │  gemini-* → gemini-webapi               │
│                   │  其他模型 → g4f                          │
│                   └────────┬────────┘                      │
└────────────────────────────┼────────────────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         ▼                   ▼                   ▼
┌─────────────────┐  ┌──────────────┐  ┌─────────────────┐
│   gemini-webapi │  │     g4f      │  │     g4f         │
│                 │  │  (聚合层)     │  │  (/v1/images)   │
│  • Gemini 2.5   │  │  • qwen      │  │  • flux         │
│  • Gemini 3.0   │  │  • kimi      │  │  • dalle-3      │
│  • Vision       │  │  • glm       │  │  • 其他          │
│  • 图像生成      │  │  • minimax   │  │                 │
│  • 文件上传      │  │  • grok      │  │                 │
└─────────────────┘  └──────────────┘  └─────────────────┘
```

### 技术栈

| 组件 | 技术 | 版本 |
|------|------|------|
| Web 框架 | FastAPI | ^0.115 |
| Gemini 库 | gemini-webapi | ^1.17.3 |
| ChatGPT 库 | g4f | latest |
| 配置管理 | Pydantic | ^2.0 |
| 文件监听 | watchdog | ^3.0 |
| 日志 | loguru | ^0.7 |
| 测试 | pytest | latest |

---

## 📡 API 端点

### OpenAI 兼容端点

| 端点 | 方法 | 描述 |
|-----|------|------|
| `/v1/models` | GET | 列出可用模型 |
| `/v1/chat/completions` | POST | 聊天完成（支持 Vision） |
| `/v1/images` | POST | 图像生成 |
| `/v1/files` | POST | 文件上传 |
| `/v1/chat/completions/with-files` | POST | 文件对话 |

### Claude 兼容端点

| 端点 | 方法 | 描述 |
|-----|------|------|
| `/v1/claude/models` | GET | 列出可用模型 |
| `/v1/messages` | POST | 消息完成 |

### 管理端点

| 端点 | 方法 | 描述 |
|-----|------|------|
| `/health` | GET | 健康检查（含 Provider 状态） |
| `/admin/config/reload` | POST | 手动触发配置重载 |
| `/admin/cookies` | POST | 更新 Cookie |
| `/admin/cookies/status` | GET | Cookie 状态查询 |
| `/admin/logging` | POST | 切换日志级别 |

---

## 🔧 配置说明

### 配置文件 (config.yaml)

```yaml
server:
  host: "0.0.0.0"
  port: 8022

auth:
  bearer_token: "your-secure-token-here"

logging:
  level: "INFO"
  format: "{time:YYYY-MM-DD HH:mm:ss} | {level: <8} | {message}"
  file: "/app/logs/gateway.log"
  rotation: "10 MB"
  retention: "7 days"

gemini:
  enabled: true
  cookie_path: "/app/cookies/gemini.json"
  auto_refresh: true
  timeout: 30
  models:
    - "gemini-2.5-pro"
    - "gemini-2.5-flash"

g4f:
  enabled: false
  base_url: "http://localhost:1337"
  timeout: 30.0
  providers: []
  model_prefixes: []
```

### 环境变量

| 变量 | 说明 | 默认值 |
|-----|------|--------|
| `CONFIG_PATH` | 配置文件路径 | - |
| `BEARER_TOKEN` | 认证令牌 | - |
| `LOG_LEVEL` | 日志级别 | INFO |
| `GEMINI_TIMEOUT` | Gemini 超时(秒) | 30 |
| `G4F_TIMEOUT` | g4f 超时(秒) | 30.0 |

### 热重载支持

支持热重载的配置项（修改后无需重启）：
- ✅ `logging.level` - 日志级别
- ✅ `auth.bearer_token` - 认证令牌
- ✅ `gemini.models` - 模型列表
- ✅ `gemini.timeout` - 超时时间
- ✅ `g4f.timeout` - 超时时间

---

## 📁 项目结构

```
ai-gateway/
├── app/
│   ├── __init__.py
│   ├── main.py                    # FastAPI 入口
│   ├── auth/
│   │   ├── __init__.py
│   │   └── middleware.py          # Bearer 认证中间件
│   ├── config/
│   │   ├── __init__.py
│   │   ├── settings.py            # Pydantic 配置模型
│   │   ├── manager.py             # 配置管理器
│   │   └── watcher.py             # 热重载观察器
│   ├── middlewares/
│   │   ├── __init__.py
│   │   └── logging.py             # 请求日志中间件
│   ├── providers/
│   │   ├── __init__.py
│   │   ├── base.py                # Provider 基类
│   │   ├── gemini.py              # Gemini Provider
│   │   └── g4f.py                 # g4f Provider
│   ├── routes/
│   │   ├── __init__.py
│   │   ├── openai.py              # OpenAI 协议路由
│   │   ├── claude.py              # Claude 协议路由
│   │   ├── admin.py               # 管理接口路由
│   │   └── files.py               # 文件上传路由
│   ├── services/
│   │   ├── __init__.py
│   │   ├── logger.py              # 日志服务
│   │   ├── model_registry.py      # 模型注册表
│   │   └── stream.py              # 流式处理
│   └── utils/
│       ├── __init__.py
│       └── errors.py              # 错误处理模块
├── tests/
│   ├── __init__.py
│   ├── test_claude_format.py      # Claude 格式测试 (6 tests)
│   ├── test_multimodal.py         # 多模态测试 (8 tests)
│   ├── test_errors.py             # 错误处理测试 (12 tests)
│   └── test_config_watcher.py     # 配置热重载测试 (6 tests)
├── docs/
│   ├── architecture.md            # 架构设计文档
│   ├── api-spec.md                # API 规范文档
│   ├── config-examples.md         # 配置示例
│   ├── deployment.md              # 部署指南
│   └── troubleshooting.md         # 故障排查
├── README.md                      # 项目说明
├── PROJECT_SUMMARY.md             # 本文件
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── .gitignore
```

---

## 🧪 测试覆盖

### 测试统计

| 测试文件 | 测试数量 | 描述 |
|---------|---------|------|
| `test_claude_format.py` | 6 | Claude ↔ OpenAI 格式转换 |
| `test_multimodal.py` | 8 | Vision、图像生成、文件上传 |
| `test_errors.py` | 12 | 错误分类和处理 |
| `test_config_watcher.py` | 6 | 配置热重载 |
| **总计** | **32** | **全部通过** ✅ |

### 运行测试

```bash
# 安装依赖
pip install -r requirements.txt

# 运行所有测试
pytest -v

# 运行特定测试
pytest tests/test_errors.py -v
```

---

## 🚀 快速开始

### Docker 部署（推荐）

```bash
# 1. 克隆项目
git clone <repo-url>
cd ai-gateway

# 2. 准备配置
mkdir -p config cookies logs
ncp docs/config-examples.md config/config.yaml
# 编辑 config.yaml，设置 bearer_token

# 3. 准备 Cookie
# 从浏览器获取 __Secure-1PSID 和 __Secure-1PSIDTS
# 写入 cookies/gemini.json

# 4. 启动
docker-compose up -d

# 5. 验证
curl http://localhost:8022/health
```

### 使用示例

**OpenAI 客户端**:
```python
from openai import OpenAI

client = OpenAI(
    base_url="http://localhost:8022/v1",
    api_key="your-bearer-token"
)

# 文本对话
response = client.chat.completions.create(
    model="gemini-2.5-pro",
    messages=[{"role": "user", "content": "Hello!"}]
)

# Vision 图像理解
response = client.chat.completions.create(
    model="gemini-2.5-pro",
    messages=[{
        "role": "user",
        "content": [
            {"type": "text", "text": "描述这张图"},
            {"type": "image_url", "image_url": {"url": "data:image/png;base64,..."}}
        ]
    }]
)
```

**Claude Code CLI**:
```bash
export ANTHROPIC_BASE_URL=http://localhost:8022
export ANTHROPIC_API_KEY=your-bearer-token
claude --model gemini-2.5-pro
```

---

## 📊 性能特性

| 特性 | 实现 |
|-----|------|
| 非流式响应 | 统一非流式，简化实现 |
| 连接池 | 复用 HTTP 连接 |
| 防抖处理 | 配置热重载 1 秒防抖 |
| 超时控制 | 可配置请求超时 |
| 错误重试 | Provider 级别错误分类 |

---

## 🔒 安全特性

| 特性 | 说明 |
|-----|------|
| Bearer 认证 | 标准 Token 认证 |
| Cookie 脱敏 | `/admin/cookies/status` 不返回真实值 |
| 请求日志 | 记录请求路径和耗时（不含敏感内容） |
| 错误脱敏 | 不暴露内部堆栈信息 |

---

## 📈 开发阶段总结

| 阶段 | 内容 | 提交数 |
|-----|------|--------|
| Phase 1 | 管理接口 (Cookie、日志、健康检查) | 5 |
| Phase 2 | Claude 协议支持 | 5 |
| Phase 3 | 多模态 (Vision、文件、图像生成) | 4 |
| Phase 4 | 稳定性 (错误处理、日志、超时) | 6 |
| Phase 5 | 配置热重载 | 5 |
| **总计** | | **25** |

---

## 📚 文档索引

- [架构设计](docs/architecture.md) - 系统架构和技术选型
- [API 规范](docs/api-spec.md) - 完整的 API 接口文档
- [配置示例](docs/config-examples.md) - 配置文件参考
- [部署指南](docs/deployment.md) - 详细部署步骤
- [故障排查](docs/troubleshooting.md) - 常见问题定位

---

## 🎯 后续建议

可选的增强功能：

1. **会话保持** - 通过 `session_id` 支持多轮对话上下文
2. **使用统计** - 请求计数、模型使用统计
3. **Web 搜索** - 透传 g4f 的 `web_search` 参数
4. **限流控制** - 基于令牌桶的速率限制
5. **Prometheus 指标** - 请求延迟、错误率等指标

---

## 📝 变更日志

### v1.0.0 (2026-02-08)

- ✅ 初始版本发布
- ✅ OpenAI / Claude 双协议支持
- ✅ Gemini + g4f 多 Provider 支持
- ✅ Vision 图像理解
- ✅ 文件上传分析
- ✅ 配置热重载
- ✅ 完整的错误处理
- ✅ 32 个单元测试

---

*Made with ❤️ for AI enthusiasts*
